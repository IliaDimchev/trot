import sys
import os

# Абсолютният път до вашето приложение
sys.path.insert(0, os.path.dirname(__file__))

from app import app as application

from app import db

with application.app_context():
    db.create_all()
